default: &default
  adapter: postgresql
  pool: 50
  timeout: 5000
  host: localhost
  user: postgres

development:
  <<: *default
  database: ci_web_development

production:
  <<: *default
  database: ci_web_development

test:
  <<: *default
  database: ci_web_test
