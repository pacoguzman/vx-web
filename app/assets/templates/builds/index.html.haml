.builds

  %a.builds-add.pull-right{ class: "btn btn-small btn-primary", 'ng-show' => "project", "ng-click" => "createBuild()" }
    Create Build

  %ul.builds-list
    %li.build-item{ 'ng-repeat' => "build in builds | orderBy:'number':true" }
      {{ build.number }}

