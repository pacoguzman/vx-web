.job
  .row.task-attributes{ 'ng-show' => 'job' }
    .col-xs-5

      %dl.task-attribute.dl-horizontal
        %dt Job
        %dd.app-task-status{ task: "job" }
          {{ task.number }}

      %dl.task-attribute.dl-horizontal
        %dt Status
        %dd {{ job.status }}

      %dl.task-attribute.dl-horizontal
        %dt Finished At
        %dd.app-task-finished{ task: "job" }

      %dl.task-attribute.dl-horizontal
        %dt Duration
        %dd.app-task-duration{ task: 'job' }

    .col-xs-5

      %dl.task-attribute.dl-horizontal
        %dt Commit
        %dd.app-build-http-url{ build: 'build', branch: 'true' }

      %dl.task-attribute.dl-horizontal
        %dt Author
        %dd
          %a{'ng-href' => "mailto:{{build.author_email}}"}
            {{ build.author }}

      %dl.task-attribute.dl-horizontal
        %dt Message
        %dd {{ build.message }}

      %dl.task-attribute.dl-horizontal
        %dt Config
        %dd{ 'ng-repeat' => "(key, value) in job.matrix" }
          {{ key }} {{ value }}

    .col-xs-2
      .pull-right
        %a{'ng-href' => "{{job.text_logs_url}}", title: "Download Text Log", target: "_blank" }
          %i.fa.fa-bars.fa-2x

  .app-task-output{ collection: "logs", 'ng-show' => 'logs.length != undefined' }

  .ajax-loader{ 'ng-hide' => 'logs.length != undefined' }

